# Runtime dependencies for the SCOPE demo (demo_app.py).
# Trimmed to exactly what the demo's import closure uses — benchmark-only
# packages (numpy, evaluate, rouge-score, nltk, tqdm, tabulate, tiktoken)
# are intentionally omitted.
streamlit>=1.30.0
pandas>=2.0.0
langchain-openai>=0.1.0
openai>=1.30.0
pydantic>=2.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
ddgs>=7.0.0
python-whois>=0.9.0
